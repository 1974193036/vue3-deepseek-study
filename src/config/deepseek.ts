// 模型配置
export const MODEL_CONFIG = {
  model: 'deepseek-chat',
  temperature: 1.0,
}

// 本地存储键名配置
export const STORAGE_KEY = {
  sessionList: 'chatproject_list',
  activeIndex: 'chatproject_index',
}
